"""Quick Data: arbitrary JSON/CSV analysis exposed over MCP and HTTP.

The analysis logic lives in :mod:`quickdata.engine` and is consumed by both
``quickdata.mcp_server`` (MCP tools + prompts) and ``quickdata.api`` (FastAPI).
"""

__version__ = "0.1.0"
